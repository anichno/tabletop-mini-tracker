[project]
name = "solve-anoto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "microdots",
]

[tool.uv.sources]
microdots = { git = "https://github.com/cheind/py-microdots" }
